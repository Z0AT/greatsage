# Memory Log - 2026-04-10

## Hover Positioning Bug - Multiple Iterations

### Initial Fix Attempt (14:00 UTC)
- Attempted to fix hover positioning - branch items didn't set `hoveredItemPos`
- Updated `onHover` to pass full item instead of just ID
- Added `setHoveredItemPos` to `InventoryItemNode` props with null checks
- Fixed onMouseEnter handlers to set hover coordinates
- Build succeeded, deployed to staging

### White Screen Result
- Site at http://10.135.30.250/dc-react/ showed white screen after hover fix
- Issue: onHover callback signature mismatch - expected `id: string`, not full item
- Reverted to original working version

### Subagent Delegation (15:41 UTC)
- User pointed out I should use subagents for coding tasks
- Spawned subagent to fix hover positioning
- Subagent ran but timed out (1m+ runtime)
- Subagent returned empty results despite "completed successfully" status

### Second Attempt (15:47 UTC)
- Applied subagent's fix approach manually
- Updated onMouseEnter handlers to pass mouse coordinates
- Build succeeded
- Deployed, site still white screen

### nginx Configuration Issue (16:02-16:30 UTC)
- Issue: nginx config pointed to `/opt/desire-cache` instead of `/opt/desire-cache-react-pivot`
- Fix: Updated alias directive from `/opt/desire-cache-react-pivot-react-pivot/` to `/opt/desire-cache-react-pivot/`
- Site now functional at http://10.135.30.250/dc-react/

### Key Lessons
1. **Agent delegation required** - Zoat explicitly corrected: always use appropriate agents (Diablo for coding, Souei for research, Geld for infrastructure, Rigurd for admin)
2. **Test locally before deploying** - React changes need to be tested to catch runtime errors
3. **Subagent issues** - Subagents can time out or return empty results despite completion status
4. **Hover positioning** - Branch items' onMouseEnter handlers need to pass coordinates, not just IDs

## Agent Settings Configuration (15:50 UTC)
- Created subagents in settings.json for agent routing
- All agents have identity files (SOUL.md, AGENTS.md, USER.md, IDENTITY.md, TOOLS.md, MEMORY.md)
- Agent bindings configured in OpenClaw

## Subagent Deletion Command (15:52 UTC)
- Command to clean up zombie subagents: `find ~/.openclaw/session -type f -name "*.json" -mtime +0 -delete`

## Hover Positioning Analysis (16:50 UTC)
- Subagents identified root cause: `position: absolute` vs viewport coordinates conflict
- CSS `.dossier-pop--fixed` had `position: fixed` but no positioning values
- Fix: Added `transform: none` to prevent conflicting transforms

## Left/Right Overlay Positioning Fix (17:13-17:25 UTC)
- **User requirement:** Overlay should pop left or right (not up/down), close to the item
- Subagent updated `DossierPlacement` type from 'above'/'below'/'center' to 'left'/'right'/'center'
- Updated `usePositionedDossier()` hook to check horizontal space instead of vertical
- Modified `getOverlayStyle()` to pop overlay right if more space right, else left
- Vertical position centered on item Y
- **Build:** index-Bq_Uz-9V.js (203.67 kB)
- **Deploy:** http://10.135.30.250/dc-react/
- **Status:** Deployed successfully
- **Issue:** Image shows overlay is still spawning far from item - subagent needs to investigate further

## Current State
- Hover positioning now opens left or right based on available space
- Overlay centered vertically on item
- Build: index-Bq_Uz-9V.js
- Site URL: http://10.135.30.250/dc-react/
- **Next:** Subagent needs to investigate why overlay is still spawning far from item
