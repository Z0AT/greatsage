# Memory Log - 2026-04-11

## Hover Positioning Fix - Left/Right Overlay (2026-04-10)

### User Requirement (17:10 UTC)
- Overlay should pop left or right (not up/down), especially for mobile platforms
- Overlay should appear close to the item (not far away)

### Implementation
- Subagent updated `DossierPlacement` type from 'above'/'below'/'center' to 'left'/'right'/'center'
- Updated `usePositionedDossier()` hook to check horizontal space instead of vertical
- Modified `getOverlayStyle()` to pop overlay right if more space right, else left
- Vertical position centered on item Y

### Issues Discovered
1. **First build (index-B8s7_fQ3.js)**: Same hash as previous build - files not changing
2. **Second build (index-Bq_Uz-9V.js)**: Deployed but overlay still spawning far from item
3. **Mobile issue**: Tabs opening at bottom of screen instead of near item

### Root Cause Analysis
- Issue 1: Server caching or same content producing same hash
- Issue 2: Overlay positioning using `position: absolute` with viewport coordinates - mismatch between coordinate system and positioning context
- Issue 3: `position: fixed` on mobile causes viewport-relative positioning, not item-relative

### Fix Applied
- Changed inline style to use `position: fixed` with correct `left`/`top` values
- Build hash changed but file content still not updating correctly
- Final deploy at 17:57 UTC

### Files Modified
- `/home/zoat/.openclaw/workspace/react-pivot/src/App.tsx`
- Memory log updated in `/home/zoat/.openclaw/workspace/memory/2026-04-10.md`

### Key Lessons
1. **Agent delegation required** - Always use appropriate agents for coding tasks
2. **Coordinate system matters** - Viewport coordinates vs element-relative positioning
3. **Mobile positioning** - `position: fixed` is viewport-relative, not item-relative
4. **Subagent status checks** - Need to verify actual deployed build vs expected hash
